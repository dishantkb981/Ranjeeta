export interface MemoryItem {
  id: number;
  image: string;
  title: string;
  date: string;
  description: string;
}

export interface LoveReasonItem {
  id: number;
  title: string;
  description: string;
  icon: string;
}